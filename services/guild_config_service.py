"""
GuildConfigService

Manages server-specific configuration settings for the bot, including
command prefixes, mute roles, logging channels, and bad word filters.

Responsibilities:
- Load and persist guild configuration to the database
- Update prefix, mute role, and logging channel
- Manage server-specific bad word lists
"""
